/*

Desarrollar una función para cada uno de las siguientes apartados:

- Que reciba por parámetro un número entero y retorne su triple.

- Que reciba por parámetro dos números enteros y retorne la multiplicación de
ambos.

Ejecutar ambas funciones e imprimir por consola su resultado.

*/

function elTriple (numero) {
    return resultado = (numero * 3)
}
console.log (elTriple(3));


function multiplicando (nA, nB) {
    return multiplica = (nA * nB);
}
console.log (multiplicando(3, 2));

